# dependencies
/node_modules
/.pnp
.pnp.*

# next.js
/.next/
/out/
next-env.d.ts

# production
/build
/dist

# misc
.DS_Store
*.pem
*.log
npm-debug.log*

# local env files (NEVER commit real secrets)
.env
.env*.local
!.env.example

# docker / local data
/docker/data/
docker-compose.override.yaml

# backups (produced by scripts/backup.sh)
/backups/

# external reference skills installed via `npx skills add` (kept local, not app code)
/.agents/
/skills-lock.json
/.claude/skills/remotion-best-practices

# testing
/coverage

# editor
.vscode/*
!.vscode/extensions.json
.idea

# drizzle
/drizzle/meta/_journal.bak

# os
Thumbs.db

# TypeScript incremental build cache
*.tsbuildinfo
